#include <bits/stdc++.h>
using namespace std;
void solve(int N)
{
  for(int i=1;i<=N;i++)
  {
    for(int j=1;j<=N;j++)
    {
      
    }
  }
}
int main(void)
{
  int tc;
  scanf("%d",&tc);
  while(tc--)
  {
    int N;
    scnaf("%d",&N);
    solve(N);
  }
}
