//Link:-https://open.kattis.com/problems/paintball
#include <iostream>
using namespace std;
typedef struct
{
   int first,second;
}info;
int main()
{
    int N,M;
    cin >> N >> M;
    info arr[M];
    for (int i = 0;i < M;i++)
    {
        cin >> arr[i].first >> arr[i].second;
        bool hit[N];
        
    }
}