//Link:-https://open.kattis.com/problems/camplunches
#include <iostream>
using namespace std;
int main()
{
    int n,sum=0;
    cin >> n;
    int s[n];
    for (int i = 0; i < n; i++) {
        cin >> s[i];
        sum+=s[i];
    }
    int k,x,a,b;
    cin >> k >> x >> a >> b;
    
}