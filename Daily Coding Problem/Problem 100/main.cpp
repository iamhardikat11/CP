#include <bits/stdc++.h>
using namespace std;
int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int n;
  cin >> n;
  vector<vector<int> > v(n,vector<int> (2));
  int cnt = 0;
  for(int i=0;i<n;i++)
  {
    for(int j=0;j<2;j++)
    {
      cin >> v[i][j];
    }
  }
  for(int i=1;i<n;i++)
  {
    
  }
}
