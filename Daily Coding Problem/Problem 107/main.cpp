#include <bits/stdc++.h>
using namespace std;

struct Node
{
  int value;
  struct Node* left;
  struct Node* right;
};
typedef struct Node TreeNode;

void insertNode(Treenode tree, int val)
{
  if()
}

int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  TreeNode tree;
  
}
