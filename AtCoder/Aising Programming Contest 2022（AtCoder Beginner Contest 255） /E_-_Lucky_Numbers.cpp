#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N,M;
    cin >> N >> M;
    vector<int> S(N);
    for(int i=0;i<N;i++) cin >> S[i];
    vector<int> X(M);
    for(int i = 0; i < M; i++) cin >> X[i];
    

}